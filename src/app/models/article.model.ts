export interface Article {
    _id ?: string,
    username ?: string,
    images: string [],
    name: string,
    description: string,
    condition: string,
    contact: string,
    price: number,
    location: string
}